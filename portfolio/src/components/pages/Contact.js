import React from 'react';

export default function Contact() {
    return (
        <section>
            <h1 id="contact">Contact</h1>
            <p>
                This section will contain a contact form that will check or email and alert the user to invalid entries.
            </p>
        </section>
    );
}